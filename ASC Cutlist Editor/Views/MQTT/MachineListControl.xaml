<UserControl x:Class="AscCutlistEditor.Views.MQTT.MachineListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:AscCutlistEditor.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Buttons for testing mock connections -->
        <WrapPanel>
            <Button
                Command="{Binding AddMockConnectionCommand}"
                Content="Add Mock Connection"
                Margin="5" />
        </WrapPanel>

        <!-- SQL Server connection settings. -->
        <WrapPanel
            Grid.Row="1">
            <Button
                Click="OpenSqlSettings"
                Content="Open Sql Settings"
                Margin="5" />
            <Button
                Command="{Binding StartListeningForConnectionsCommand}"
                Content="Start Listening For Connections"
                Margin="5" />
            <Button
                Command="{Binding RefreshConnectionTabsCommand}"
                Content="Clear Connection Tabs"
                Margin="5" />
        </WrapPanel>

        <TabControl
            Grid.Row="2"
            ItemsSource="{Binding
            MachineConnectionsViewModel.MachineConnectionTabs}">
        </TabControl>
    </Grid>
</UserControl>