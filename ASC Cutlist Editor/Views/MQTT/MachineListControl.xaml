<UserControl x:Class="AscCutlistEditor.Views.MQTT.MachineListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:AscCutlistEditor.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Buttons for testing mock connections -->
        <WrapPanel>
            <Button
                Command="{Binding StartListeningCommand}"
                Margin="10">
                Start Listening For Connections
            </Button>
            <Button
                Command="{Binding AddMockConnectionCommand}"
                Margin="10">
                Add Mock Connection
            </Button>

            <!-- SQL Server connection settings. -->
            <Button
                Command="{Binding ConnectToSqlServerCommand}"
                Margin="10">
                Test SQL Server Connection
            </Button>
            <!-- TODO: change expander to show dialog window -->
            <Expander>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Label>Data Source:</Label>
                    <TextBox
                             Grid.Column="1"
                             Text="{Binding SettingsViewModel[DataSource]}"
                             Margin="0,0,0,10"></TextBox>

                    <Label Grid.Row="1">Database Name:</Label>
                    <TextBox Grid.Row="1"
                             Grid.Column="1"
                             Margin="0,0,0,10"></TextBox>

                    <Label Grid.Row="2">Username:</Label>
                    <TextBox Grid.Row="2"
                             Grid.Column="1"
                             Margin="0,0,0,10"></TextBox>

                    <Label Grid.Row="3">Password:</Label>
                    <TextBox Grid.Row="3"
                             Grid.Column="1"
                             Margin="0,0,0,10"></TextBox>
                </Grid>
            </Expander>
        </WrapPanel>

        <TabControl
            Grid.Row="1"
            ItemsSource="{Binding
            MachineConnectionsViewModel.MachineConnectionTabs}">
        </TabControl>
    </Grid>
</UserControl>